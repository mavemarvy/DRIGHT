"use client";
import { useEffect, useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function CommissionPage(){const s=createClient();const [rows,setRows]=useState<any[]>([]);const [q,setQ]=useState("");const [status,setStatus]=useState("all");const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const {data:{user}}=await s.auth.getUser();if(!user){location.href="/login?next=/affiliate/commissions";return}const r=await s.from("commissions").select("*").eq("affiliate_user_id",user.id).order("created_at",{ascending:false});setRows(r.data||[]);setLoading(false)})()},[]);const filtered=rows.filter(r=>(status==="all"||r.status===status)&&JSON.stringify(r).toLowerCase().includes(q.toLowerCase()));return <main className="mx-auto max-w-6xl px-4 py-8"><h1 className="text-3xl font-semibold">Affiliate Commissions</h1><p className="mt-2 text-sm text-[var(--muted)]">Track pending, available, paid and reversed earnings.</p><div className="mt-6 flex gap-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search commission, order or listing ID" className="flex-1 rounded-xl border border-[var(--border)] bg-[var(--surface)] px-4 py-3 text-sm"/><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border border-[var(--border)] bg-[var(--surface)] px-3"><option value="all">All</option><option>pending</option><option>available</option><option>paid</option><option>reversed</option></select></div><section className="mt-6 overflow-x-auto rounded-2xl border border-[var(--border)] bg-[var(--surface)]"><table className="w-full text-left text-sm"><thead><tr className="border-b border-[var(--border)]"><th className="p-4">Commission ID</th><th>Order</th><th>Amount</th><th>Status</th><th className="p-4">Date</th></tr></thead><tbody>{loading?<tr><td className="p-6" colSpan={5}>Loading…</td></tr>:filtered.map(r=><tr className="border-b border-[var(--border)]" key={r.id}><td className="p-4">{r.commission_id||r.id}</td><td>{r.order_id||"—"}</td><td>{r.amount} {r.currency_code||"USD"}</td><td>{r.status}</td><td className="p-4">{r.created_at?new Date(r.created_at).toLocaleString():"—"}</td></tr>)}</tbody></table>{!loading&&!filtered.length&&<p className="p-8 text-sm text-[var(--muted)]">No commissions found.</p>}</section></main>}
