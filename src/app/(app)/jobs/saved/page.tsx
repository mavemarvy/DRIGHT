import { SavedJobsWorkspace } from "@/components/jobs/job-workspaces";

export const metadata = { title: "Saved Jobs | DRIGHT" };

export default function SavedJobsPage() { return <SavedJobsWorkspace />; }
