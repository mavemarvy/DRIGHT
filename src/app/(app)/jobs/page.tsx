import JobsMarketplace from "@/components/jobs/jobs-marketplace";

export const metadata = { title: "Jobs | DRIGHT", description: "Discover real jobs on DRIGHT." };

export default function JobsPage() {
  return <JobsMarketplace />;
}
