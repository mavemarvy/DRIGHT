export default function PrivacyPage() {
  return <LegalPage title="Privacy Policy" intro="DRIGHT is designed around privacy-aware profiles, database-level access controls and transparent use of personal information." sections={["Information collected and account data", "Profile visibility and public information", "How DRIGHT uses information", "Search, recommendations and personalization", "Payments, verification and fraud prevention", "Messages, reports and support", "Cookies and local storage", "Data retention, security and user rights", "International processing and updates"]} />;
}
function LegalPage({ title, intro, sections }: { title: string; intro: string; sections: string[] }) { return <main className="min-h-screen bg-[var(--background)] px-4 py-12 text-[var(--foreground)] sm:px-6"><article className="mx-auto max-w-3xl rounded-3xl border border-[var(--border)] bg-[var(--surface)] p-7 sm:p-10"><p className="text-sm font-semibold text-[var(--muted)]">DRIGHT</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">{title}</h1><p className="mt-4 leading-7 text-[var(--muted)]">{intro} The production policy should be reviewed and approved by DRIGHT before launch.</p><div className="mt-8 space-y-7">{sections.map((section) => <section key={section}><h2 className="text-lg font-semibold">{section}</h2><p className="mt-2 text-sm leading-6 text-[var(--muted)]">DRIGHT will publish the approved policy for this area here. Sensitive information is intended to remain protected by database RLS and permission controls.</p></section>)}</div></article></main>; }
